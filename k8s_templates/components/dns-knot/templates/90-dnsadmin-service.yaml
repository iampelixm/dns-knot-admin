apiVersion: v1
kind: Service
metadata:
  name: dnsadmin
  namespace: {{NAMESPACE}}
  labels:
    app: dnsadmin
spec:
  selector:
    app: dnsadmin
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
